#include <stdio.h>
#include <ctype.h>

int main() {
    char c;
    printf("Enter character: ");
    scanf(" %c", &c);
    c = tolower(c);
    if (c == 'a' || c == 'e' || c == 'i' || c == 'o' || c == 'u')
        printf("Vowel");
    else
        printf("Consonant");
    return 0;
}
