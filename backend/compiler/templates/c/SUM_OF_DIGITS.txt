#include <stdio.h>
#include <stdlib.h>

int main() {
    long long n;
    int sum = 0;
    printf("Enter number: ");
    scanf("%lld", &n);
    if(n < 0) n = -n;
    while (n != 0) {
        sum += n % 10;
        n /= 10;
    }
    printf("Sum of digits: %d", sum);
    return 0;
}
